/*
 * Java bean class for entity table ${beanClass.sqlTable} 
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 */

package ${target.javaPackageFromFolder($project.srcFolder)};

import java.util.List;
import org.hibernate.criterion.Restrictions;
import org.springframework.stereotype.Repository;
import ${BASE_PKG}.domain.${beanClass.name};
import ${BASE_PKG}.dao.${beanClass.name}Dao;
import ${BASE_PKG}.dao.abstracts.GenericHibernateDao;

#foreach( $import in $beanClass.imports )
import $import;
#end

/**
 * Data Access Object implementation for table "${beanClass.sqlTable}"
 * 
 * @author Telosys Tools Generator
 *
 */
#foreach( $field in $beanClass.keyAttributes )
	#set( $primaryKeyType = $field.formatedType(0) )
#end
@Repository
public class ${beanClass.name}HibernateDao extends GenericHibernateDao<${beanClass.name},$primaryKeyType>  implements ${beanClass.name}Dao
{
    
#foreach( $field in $beanClass.nonKeyAttributes )
	/**
	 * Find ${beanClass.name} by ${field.formatedName(0)}
	 */
	public List<${beanClass.name}> findBy${field.formatedName(0)}($field.formatedType(0) ${field.formatedName(0)}) {
		return findByCriteria(Restrictions.eq("${field.formatedName(0)}", ${field.formatedName(0)}));
	}
	
#end
}