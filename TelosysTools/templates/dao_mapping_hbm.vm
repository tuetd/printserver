<!--
  Java bean class for entity table ${beanClass.sqlTable} 
  Created on $today.date ( Time $today.time )
  Generated by $generator.name ( version $generator.version )
-->

<!DOCTYPE hibernate-mapping PUBLIC
	"-//Hibernate/Hibernate Mapping DTD 3.0//EN"
	"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd"> 
	
<hibernate-mapping>
	<class name="${BASE_PKG}.domain.${beanClass.name}" table="${beanClass.sqlTable}">
		
	#foreach( $field in $beanClass.keyAttributes )
		<id name="$field.formatedName(0)" column="$field.databaseName">
				<generator class="native"/>
			</id>
	#end
	
	#foreach( $field in $beanClass.nonKeyAttributes )
		<property name="$field.formatedName(0)" column="$field.databaseName"/>
	#end
	
	</class>

</hibernate-mapping>